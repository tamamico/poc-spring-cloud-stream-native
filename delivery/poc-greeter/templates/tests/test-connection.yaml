apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poc-greeter.fullname" . }}-test-connection"
  labels:
    {{- include "poc-greeter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poc-greeter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
